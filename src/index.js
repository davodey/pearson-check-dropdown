import React from 'react';
import ReactDOM from 'react-dom';
import LabelSelectContainer from './LabelSelectContainer';

import '../public/css/style.css';
ReactDOM.render(
  <LabelSelectContainer
    data={[]}
  />,
  document.getElementById('root')
);
